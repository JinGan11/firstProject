<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ucar.combination.dao.UserDao">

    <!--<resultMap id="baseUserResultMap" type="com.ucar.combination.model.User">
        <id column="ID" property="" jdbcType="BIGINT" />
        <result column="ACCOUNT_NAME" property="accountName" jdbcType="VARCHAR" />
        <result column="ACCOUNT_PASSWORD" property="accountPassword" jdbcType="VARCHAR" />
        <result column="STAFF_ID" property="staffId" jdbcType="BIGINT" />
        <result column="PERMISSIONS" property="permissions" jdbcType="TINYINT" />
        <result column="ACCOUNT_SECRET_EMAIL" property="accountSecretEmail" jdbcType="TINYINT" />
        <result column="CREATE_TIME" property="createTime" jdbcType="DATETIME" />
        <result column="CREATE_EMP" property="createEmp" jdbcType="BIGINT" />
        <result column="MODIFY_TIME" property="modifyTime" jdbcType="DATETIME" />
        <result column="MODIFY_EMP" property="modifyEmp" jdbcType="BIGINT" />
        <result column="ACCOUNT_STATE" property="accountState" jdbcType="TINYINT" />
        <result column="ACCOUNT_REMARK" property="accountRemark" jdbcType="VARCHAR" />
    </resultMap>-->

    <select id="qryAccountByAccountName" parameterType="String" resultType="com.ucar.combination.model.User">
        SELECT
        id, account_name, account_password, staff_id, permissions, account_secret_email, create_time, create_emp,
        modify_time, modify_emp, account_state, account_remark
        FROM t_account
        where account_name = #{accountName}
    </select>

    <select id="qryAccountPwdByAccountName" parameterType="String" resultType="com.ucar.combination.model.User">
        SELECT
        account_name,account_password
        FROM t_account
        where account_name = #{accountName}
    </select>

    <select id="qryHistoryPwdById" parameterType="com.ucar.combination.model.User" resultType="com.ucar.combination.model.HisPassword">
        SELECT
        id,account_id, history_password, create_time, create_emp, modify_time, modify_emp
        FROM t_history_password
        where account_id = #{id}
        order by create_time desc
    </select>

    <update id="updatePwdById" parameterType="com.ucar.combination.model.User">
        update t_account
        set account_password = #{accountPassword}
        where account_name = #{accountName} and id = #{id}
    </update>

    <insert id="insertHisPwd" parameterType="com.ucar.combination.model.User">
        insert into t_history_password(account_id,history_password, create_time, create_emp, modify_time, modify_emp)
        value(#{id}, #{accountPassword}, NOW(), #{staffId}, NOW(), #{staffId})
    </insert>

</mapper>
